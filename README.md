# CoolPythonApp

A simple Python application created for learning Python, virtual environments, and VS Code development workflow.

## 📦 Features

- Uses a dedicated virtual environment (`.venv`)
- Demonstrates importing libraries such as:
  - NumPy
  - Pandas
  - Matplotlib
- Has a clean project structure (`src`, `tests`, `data`, `notebooks`)
- Easy to run with a single command

---

## 🚀 Getting Started

### 1. Clone or download this project

If downloaded manually, simply open the folder in VS Code.

### 2. Create a virtual environment

```bash
python -m venv .venv
